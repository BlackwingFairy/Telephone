<Page x:Class="Telephone.PersonalPhonesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Telephone"
      mc:Ignorable="d" 
      HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="20,0"
      Title="PersonalPhonesView" Loaded="Page_Loaded">

    <Grid>
        <DataGrid AutoGenerateColumns="False" x:Name="personalGrid" Width="860">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding PhoneNumber}" Header="Номер телефона" Width="150"/>
                <DataGridTextColumn Binding="{Binding Address}" Header="Адрес" Width="250"/>
                <DataGridTextColumn Binding="{Binding Surname}" Header="Фамилия" Width="150"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Имя" Width="150"/>
                <DataGridTextColumn Binding="{Binding Patronymic}" Header="Отчество" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
