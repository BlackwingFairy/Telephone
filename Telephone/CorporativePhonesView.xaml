<Page x:Class="Telephone.CorporativePhonesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Telephone"
      mc:Ignorable="d" 
      HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="20,0"
      Title="CorporativePhonesView" Loaded="Page_Loaded">

    <Grid>
        <DataGrid AutoGenerateColumns="False" x:Name="corporativeGrid" Width="810">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding PhoneNumber}" Header="Номер телефона" Width="150"/>
                <DataGridTextColumn Binding="{Binding Address}" Header="Адрес" Width="250"/>
                <DataGridTextColumn Binding="{Binding Company}" Header="Название компании" Width="250"/>
                <DataGridTextColumn Binding="{Binding Occupation}" Header="Род деятельности" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
